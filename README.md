# portif-lio
Como rodar este projeto localmente:

Ligue o Apache e o MySQL usando o XAMPP.

Acesse o phpMyAdmin, crie um banco chamado portfolio_db e importe o arquivo portfolio_db.sql localizado na pasta database/.

Abra o terminal na pasta, instale as dependências necessárias e rode o servidor executando python app.py.